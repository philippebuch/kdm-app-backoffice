.rvo-page-section
    .rvo-toolbar 
        .rvo-toolbar-nav
            ul
                li
                    a.rvo-current(href="#") Véhicules en cours de rachat garage (23)
                li
                    a(href="#") Historique
        .rvo-buttons
            button.rvo-button-bordered.rvo-disabled(type="button") Retour transport
            button.rvo-button-bordered.rvo-disabled(type="button") Déclarer vendu
include ../../shared/filters.pug
.rvo-page-section.rvo-margin-t15
    .rvo-table-scroll
        table.rvo-table.rvo-bordered.rvo-padding-m
            +table-header()
                each header in jsondata.venteProRachatGarage.headers
                    +table-header-cell(header.label, header.align, header.fittocell, header.checkbox, header.fontsize, header.colspan, header.sort, header.filter, header.stickycol, header.cellClasses)
            +table-body()
                each row, index in jsondata.venteProRachatGarage.rows
                    +table-body-row()
                        each cell in jsondata.venteProRachatGarage.rows[index].cells
                            +table-body-cell(cell.label, cell.align, cell.toggle, cell.menu, cell.menuposition, cell.menuicon, cell.status, cell.checkbox, cell.minibutton, cell.minibuttontitle, cell.notif, cell.fontsize, cell.colspan, cell.field, cell.alwaysShow, cell.radio, cell.validation, cell.labelClasses, cell.cellClasses, cell.stickycol, cell.islink, cell.icon, cell.iconPosition, cell.dropdown, cell.dropdownTrigger, cell.dropdownOptions, radioName, cell.miniButtons, cell.tooltip)
